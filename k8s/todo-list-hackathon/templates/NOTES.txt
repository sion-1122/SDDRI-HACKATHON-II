Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

ACCESSING THE APPLICATION:

The application has been deployed to your Kubernetes cluster.

Frontend Service: {{ include "todo-list-hackathon.fullname" . }}-frontend
Backend Service: {{ include "todo-list-hackathon.fullname" . }}-backend

To access the application, you can use port-forwarding:

  # Forward frontend to localhost:8080
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "todo-list-hackathon.fullname" . }}-frontend 8080:80

  # Forward backend to localhost:8000
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "todo-list-hackathon.fullname" . }}-backend 8000:8000

Then open your browser to: http://localhost:8080

For Minikube, you can also use:

  # Get frontend URL
  minikube service -n {{ .Release.Namespace }} {{ include "todo-list-hackathon.fullname" . }}-frontend --url

UPGRADING:

To upgrade the release:

  $ helm upgrade {{ .Release.Name }} . -f values-local.yaml

ROLLBACK:

To rollback to a previous revision:

  $ helm rollback {{ .Release.Name }}

UNINSTALL:

To uninstall the release:

  $ helm uninstall {{ .Release.Name }}
