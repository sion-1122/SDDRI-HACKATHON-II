name: hackathon-todo
version: "1.0"

structure:
  specs_dir: specs
  features_dir: specs/features
  api_dir: specs/api
  database_dir: specs/database
  ui_dir: specs/ui

phases:
  - name: phase1-console
    description: In-memory Python console application
    features:
      - task-crud

  - name: phase2-web
    description: Full-stack web application with authentication
    features:
      - task-crud
      - authentication

  - name: phase3-chatbot
    description: AI-powered chatbot using MCP and Agents SDK
    features:
      - task-crud
      - authentication
      - chatbot