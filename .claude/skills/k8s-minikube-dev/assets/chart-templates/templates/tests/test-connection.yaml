apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todo-list-hackathon.fullname" $ }}-test-connection"
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todo-list-hackathon.fullname" $ }}-{{ .Values.component.name }}:{{ .Values.service.port }}{{ .Values.probes.liveness.path }}']
  restartPolicy: Never
